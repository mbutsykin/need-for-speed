# Fail installs if the lockfile is out of date in CI.
# strict-peer-dependencies is off to avoid friction with the wider ecosystem.
strict-peer-dependencies=false
# Keep a single, deterministic node_modules layout across the workspace.
shared-workspace-lockfile=true
